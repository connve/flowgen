stages:
  - build

rust-latest:
  stage: build
  image: rust:latest
  script:
    - chmod +x ./scripts/*.sh
    - ./scripts/build.sh

rust-nightly:
  stage: build
  image: rustlang/rust:nightly
  script:
    - chmod +x ./scripts/*.sh
    - ./scripts/build.sh
  allow_failure: true