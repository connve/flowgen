{{- $worker := index .Values "flowgen" -}}
{{- if and $worker.enabled $worker.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "flowgen.serviceAccountName" . }}
  labels:
    {{- include "flowgen.labels" . | nindent 4 }}
  {{- with $worker.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
