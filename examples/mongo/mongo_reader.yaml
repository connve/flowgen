flow:
  name: "mongo_read"
  tasks:
     # Run this task on specified intervals.
    - generate:
        name: "generate_jobs"
        interval: 0s
        count: 2

    - "mongo_reader":
        name: "mongo_reader"